{
  "name": "academy",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "dev:backend": "pnpm --filter @workspace/api-server run dev",
    "dev:frontend": "cross-env PORT=5173 BASE_PATH=/ pnpm --filter @workspace/academy run dev",
    "dev": "concurrently --kill-others-on-fail \"pnpm run dev:backend\" \"pnpm run dev:frontend\""
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1",
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3"
  }
}
